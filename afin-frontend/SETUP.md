# Инструкция по настройке и запуску afin

## ⚠️ Важно: Пошаговая инструкция

### Шаг 0: Запуск Docker Desktop

**ОБЯЗАТЕЛЬНО:** Перед выполнением следующих шагов убедитесь, что Docker Desktop запущен и работает.

1. Найдите и запустите "Docker Desktop" из меню Пуск
2. Дождитесь полной загрузки (иконка в трее перестанет анимироваться)
3. Проверьте работу командой:
   ```powershell
   docker ps
   ```
   Должна вернуться пустая таблица без ошибок.

### Шаг 1: Создание файла .env

**Windows (PowerShell):**
```powershell
cd backend
if (-not (Test-Path .env)) {
    Copy-Item .env.example .env
}
cd ..
```

**Linux/Mac:**
```bash
cd backend
cp .env.example .env
cd ..
```

**Или вручную:** Создайте файл `backend/.env` со следующим содержимым:
```
DATABASE_URL="postgresql://afin_user:afin_password@localhost:5432/afin_db"
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production
NODE_ENV=development
PORT=5000
```

### Шаг 2: Запуск Docker

```bash
docker-compose up --build
```

Это может занять несколько минут при первом запуске, так как:
- Скачиваются образы (postgres, node)
- Устанавливаются зависимости
- Собираются приложения

### Шаг 3: Ожидание готовности

Дождитесь сообщений:
- `🚀 Сервер запущен на порту 5000` (backend)
- `afin-postgres` готов (postgres)

### Шаг 4: Выполнение миграций и заполнение базы данных

В **новом терминале** выполните:

```bash
# Выполнить миграции
docker exec -it afin-backend npx prisma migrate deploy

# Заполнить базу данных тестовыми данными
docker exec -it afin-backend npm run db:seed
```

Вы должны увидеть:
```
🌱 Начало заполнения базы данных...
✅ Пользователи созданы
✅ Модели процессов созданы
✅ Симуляции созданы
✅ Аналитика создана
🎉 Заполнение базы данных завершено!
```

### Шаг 5: Открытие приложения

Откройте в браузере:
- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:5000/api/health

### Шаг 6: Вход в систему

Используйте тестовые учетные записи:
- Email: `admin@afin.ru`
- Пароль: `password123`

## 🔧 Решение проблем

### Проблема: "Cannot connect to database"

**Решение:**
1. Убедитесь, что контейнер postgres запущен: `docker ps`
2. Проверьте логи: `docker logs afin-postgres`
3. Перезапустите: `docker-compose restart postgres`

### Проблема: "Migration failed"

**Решение:**
```bash
# Остановите контейнеры
docker-compose down

# Удалите volumes (ОСТОРОЖНО: удалит данные!)
docker-compose down -v

# Запустите заново
docker-compose up --build
```

### Проблема: "Port already in use"

**Решение:**
Измените порты в `docker-compose.yml`:
```yaml
ports:
  - "3001:80"  # вместо 3000:80 для frontend
  - "5001:5000"  # вместо 5000:5000 для backend
```

### Проблема: "Module not found" или ошибки сборки

**Решение:**
```bash
# Пересоберите образы без кеша
docker-compose build --no-cache

# Запустите заново
docker-compose up
```

## 📝 Проверка работоспособности

### Проверка backend:
```bash
curl http://localhost:5000/api/health
```

Должен вернуть: `{"status":"ok","message":"Сервер работает"}`

### Проверка frontend:
Откройте http://localhost:3000 - должна загрузиться главная страница

### Проверка базы данных:
```bash
docker exec -it afin-postgres psql -U afin_user -d afin_db -c "SELECT COUNT(*) FROM users;"
```

Должно вернуть число пользователей (после seed должно быть 2)

## 🛑 Остановка приложения

```bash
# Остановить контейнеры
docker-compose stop

# Остановить и удалить контейнеры
docker-compose down

# Остановить, удалить контейнеры и volumes (удалит данные БД!)
docker-compose down -v
```

## 🔄 Перезапуск после изменений

```bash
# Перезапуск без пересборки
docker-compose restart

# Перезапуск с пересборкой
docker-compose up --build
```

