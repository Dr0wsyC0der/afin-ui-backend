# afin - Система управления бизнес-процессами

Полнофункциональное современное веб-приложение для бизнес-процесс моделинга, симуляции и аналитики.

## 🚀 Технологический стек

### Frontend
- React 18 + TypeScript
- Vite
- Tailwind CSS v3
- TanStack React Table
- Recharts
- React Flow (для BPMN редактора)
- React Router DOM
- Axios

### Backend
- Node.js + Express
- TypeScript
- PostgreSQL
- Prisma ORM
- JWT аутентификация (access + refresh tokens)
- bcryptjs для хеширования паролей

### Инфраструктура
- Docker + Docker Compose
- Nginx (для production frontend)

## 📋 Функциональность

### Аутентификация
- ✅ Регистрация пользователей
- ✅ Вход в систему
- ✅ JWT токены с refresh механизмом
- ✅ Защищенные маршруты

### Панель управления (Dashboard)
- ✅ Ключевые метрики (активные процессы, ожидающие утверждения, завершенные задачи, среднее время цикла)
- ✅ Ежемесячное выполнение процессов (столбчатая диаграмма)
- ✅ Обзор моделей процессов (круговая диаграмма)

### Модели процессов
- ✅ Таблица со всеми моделями
- ✅ Поиск и фильтрация
- ✅ Создание новых моделей
- ✅ Редактирование, копирование, удаление
- ✅ BPMN редактор с drag-and-drop
- ✅ Панель инструментов (Задача, Шлюз, События)
- ✅ Панель свойств элементов

### Симуляции
- ✅ Выбор модели процесса
- ✅ Настройка параметров (длительность, доступность ресурсов, интенсивность)
- ✅ Запуск симуляций
- ✅ Отображение метрик (пропускная способность, время цикла, стоимость, утилизация)
- ✅ Обнаружение узких мест
- ✅ Сравнение сценариев

### Аналитика
- ✅ Сводные метрики
- ✅ Ежемесячные завершения процессов (линейный + площадной график)
- ✅ Утилизация ресурсов по процессу
- ✅ Анализ узких мест

### Профиль пользователя
- ✅ Редактирование личных данных
- ✅ Изменение пароля
- ✅ Настройки уведомлений
- ✅ Настройки приложения

## 🛠️ Установка и запуск

### Предварительные требования

- Node.js 20+
- Docker и Docker Compose
- PostgreSQL (или используйте Docker)

### Локальный запуск (без Docker)

#### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd afin-ui
```

#### 2. Установка зависимостей

```bash
# Установка зависимостей для всех проектов
npm run install:all
```

Или вручную:

```bash
# Backend
cd backend
npm install
cd ..

# Frontend
cd frontend
npm install
cd ..
```

#### 3. Настройка базы данных

Создайте файл `backend/.env` на основе `backend/.env.example`:

```bash
cd backend
cp .env.example .env
```

Отредактируйте `backend/.env`:

```env
DATABASE_URL="postgresql://afin_user:afin_password@localhost:5432/afin_db"
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production
NODE_ENV=development
PORT=5000
```

#### 4. Настройка Prisma

```bash
cd backend
npx prisma generate
npx prisma migrate dev
npx prisma db seed
```

#### 5. Запуск приложения

В корневой директории:

```bash
npm run dev
```

Или отдельно:

```bash
# Terminal 1 - Backend
cd backend
npm run dev

# Terminal 2 - Frontend
cd frontend
npm run dev
```

Приложение будет доступно:
- Frontend: http://localhost:3000
- Backend API: http://localhost:5000

### Запуск с Docker

#### 1. Клонирование и подготовка

```bash
git clone <repository-url>
cd afin-ui
```

#### 2. Настройка переменных окружения

Создайте файл `backend/.env` из примера:

**Windows (PowerShell):**
```powershell
Copy-Item backend\.env.example backend\.env
```

**Linux/Mac:**
```bash
cp backend/.env.example backend/.env
```

Или создайте вручную файл `backend/.env` со следующим содержимым:
```env
DATABASE_URL="postgresql://afin_user:afin_password@postgres:5432/afin_db"
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production
NODE_ENV=production
PORT=5000
```

**Примечание:** Для Docker переменные окружения уже настроены в `docker-compose.yml`, но файл `.env` нужен для локальной разработки.

#### 3. Запуск с Docker Compose

```bash
# Сборка и запуск всех сервисов
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

#### 4. Инициализация базы данных

После запуска контейнеров выполните миграции и seed:

```bash
# Выполнить миграции
docker exec -it afin-backend npx prisma migrate deploy

# Выполнить seed данных
docker exec -it afin-backend npm run db:seed
```

Или вручную:

```bash
# Войти в контейнер backend
docker exec -it afin-backend sh

# Внутри контейнера
npx prisma migrate deploy
npm run db:seed
```

#### 5. Доступ к приложению

- Frontend: http://localhost:3000
- Backend API: http://localhost:5000

### Остановка Docker контейнеров

```bash
docker-compose down

# С удалением volumes (удалит данные БД)
docker-compose down -v
```

## 👤 Тестовые учетные записи

После выполнения seed данных доступны следующие учетные записи:

- **Администратор:**
  - Email: `admin@afin.ru`
  - Пароль: `password123`

- **Пользователь:**
  - Email: `user@afin.ru`
  - Пароль: `password123`

## 📁 Структура проекта

```
afin-ui/
├── backend/                 # Backend приложение
│   ├── src/
│   │   ├── routes/         # API маршруты
│   │   ├── middleware/     # Middleware (auth)
│   │   ├── utils/          # Утилиты (JWT)
│   │   ├── prisma/         # Prisma клиент и seed
│   │   └── index.ts        # Точка входа
│   ├── prisma/
│   │   └── schema.prisma   # Схема базы данных
│   ├── Dockerfile
│   └── package.json
│
├── frontend/               # Frontend приложение
│   ├── src/
│   │   ├── pages/         # Страницы приложения
│   │   ├── components/    # React компоненты
│   │   ├── contexts/      # React контексты (Auth)
│   │   └── App.tsx        # Главный компонент
│   ├── Dockerfile
│   ├── nginx.conf
│   └── package.json
│
├── docker-compose.yml      # Docker Compose конфигурация
└── README.md
```

## 🔧 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/refresh` - Обновление токена
- `POST /api/auth/logout` - Выход
- `GET /api/auth/me` - Текущий пользователь

### Модели процессов
- `GET /api/process-models` - Список моделей
- `GET /api/process-models/:id` - Получить модель
- `POST /api/process-models` - Создать модель
- `PUT /api/process-models/:id` - Обновить модель
- `DELETE /api/process-models/:id` - Удалить модель
- `POST /api/process-models/:id/copy` - Копировать модель

### Симуляции
- `GET /api/simulations` - Список симуляций
- `GET /api/simulations/:id` - Получить симуляцию
- `POST /api/simulations` - Создать симуляцию

### Аналитика
- `GET /api/analytics` - Список аналитики
- `GET /api/analytics/summary` - Сводная аналитика
- `POST /api/analytics` - Создать запись аналитики

### Пользователи
- `GET /api/users/profile` - Профиль пользователя
- `PUT /api/users/profile` - Обновить профиль
- `PUT /api/users/password` - Изменить пароль

### Dashboard
- `GET /api/dashboard` - Данные для дашборда

## 🎨 Дизайн

- **Цветовая схема:**
  - Основной: `#0055FF` (синий)
  - Акценты: `#FF3B30` (красный), `#34C759` (зеленый)

- **Шрифты:** Inter, Manrope

- **Иконки:** Lucide React

- **Адаптивность:** Полностью адаптивный дизайн (mobile-friendly)

## 🔒 Безопасность

- Пароли хешируются с помощью bcryptjs
- JWT токены для аутентификации
- Refresh tokens для обновления сессий
- Защита маршрутов через middleware
- Валидация входных данных

## 📝 Разработка

### Добавление новых миграций Prisma

```bash
cd backend
npx prisma migrate dev --name migration_name
```

### Просмотр базы данных

```bash
cd backend
npx prisma studio
```

### Сборка для production

```bash
# Backend
cd backend
npm run build

# Frontend
cd frontend
npm run build
```

## 🐛 Решение проблем

### Проблемы с подключением к БД

Убедитесь, что:
1. PostgreSQL запущен
2. Правильные credentials в `.env`
3. База данных создана

### Проблемы с Docker

```bash
# Очистка и пересборка
docker-compose down -v
docker-compose build --no-cache
docker-compose up
```

### Проблемы с зависимостями

```bash
# Удаление node_modules и переустановка
rm -rf node_modules frontend/node_modules backend/node_modules
npm run install:all
```

## 📄 Лицензия

MIT

## 👥 Авторы

Создано для управления бизнес-процессами

---

**Примечание:** В production обязательно измените секретные ключи JWT и используйте безопасные пароли для базы данных!

