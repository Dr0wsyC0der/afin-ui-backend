# Решение проблем с Docker

## ❌ Ошибка: "The system cannot find the file specified"

Эта ошибка означает, что **Docker Desktop не запущен**.

### Решение:

1. **Запустите Docker Desktop:**
   - Найдите "Docker Desktop" в меню Пуск
   - Или найдите иконку Docker в системном трее
   - Запустите приложение

2. **Дождитесь полного запуска:**
   - В трее появится иконка Docker (кит)
   - Иконка перестанет анимироваться, когда Docker будет готов
   - Обычно это занимает 30-60 секунд

3. **Проверьте, что Docker работает:**
   ```powershell
   docker ps
   ```
   Должна вернуться пустая таблица (без ошибок)

4. **Запустите docker-compose снова:**
   ```powershell
   docker-compose up --build
   ```

## 🔍 Другие распространенные проблемы

### Проблема: Docker Desktop не запускается

**Решение:**
1. Перезагрузите компьютер
2. Убедитесь, что включена виртуализация в BIOS
3. Проверьте, что WSL2 установлен (для Windows)
4. Переустановите Docker Desktop

### Проблема: "WSL 2 installation is incomplete"

**Решение:**
1. Установите WSL2:
   ```powershell
   wsl --install
   ```
2. Перезагрузите компьютер
3. Запустите Docker Desktop снова

### Проблема: Docker Desktop зависает при запуске

**Решение:**
1. Закройте Docker Desktop полностью
2. Откройте Диспетчер задач (Ctrl+Shift+Esc)
3. Завершите все процессы Docker
4. Запустите Docker Desktop от имени администратора

### Проблема: Порты заняты

**Решение:**
Измените порты в `docker-compose.yml`:
```yaml
ports:
  - "3001:80"    # вместо 3000
  - "5001:5000"  # вместо 5000
  - "5433:5432"  # вместо 5432 для postgres
```

## ✅ Проверка работоспособности Docker

```powershell
# Проверка версии
docker --version

# Проверка статуса
docker ps

# Проверка образов
docker images

# Проверка docker-compose
docker-compose --version
```

Все команды должны выполняться без ошибок.

## 🚀 После запуска Docker Desktop

Когда Docker Desktop запущен и готов:

```powershell
# 1. Проверьте статус
docker ps

# 2. Запустите проект
docker-compose up --build

# 3. В другом терминале выполните seed
docker exec -it afin-backend npm run db:seed
```

